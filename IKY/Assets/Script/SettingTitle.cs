using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class SettingTitle : MonoBehaviour
{
 /*   
    public class Countent {
    public string title;
    public string date;
    public string message;
    public string user;

    public Countent() {
    }

    public Countent(string title, string date, string message, string user) {
        this.title = title;
        this.date = date;
        this.message = message;
        this.user = user;
    }

    private void writeNewUser(string title, string date, string message, string user) {
    Countent countent = new Countent(title, date, message, user);
    string json = JsonUtility.ToJson(user);

    mDatabaseRef.Child("users").Child(userId).SetRawJsonValueAsync(json);
}

}*/
/*
      InputField titleInputField;
 
    void Start() {
 
        titleInputField = GetComponent<InputField>();
 
        InitInputField();
    }
    public void InputLogger() {
 
        string inputValue = titleInputField.text;
 
        Debug.Log("タイトルは" + inputValue);
 
        InitInputField();
    }

    void InitInputField() {
        // フォーカス
        titleInputField.ActivateInputField();
    }*/
}

